# Backend base URL (used when NOT using Vite dev proxy)
VITE_API_BASE_URL=http://localhost:8080

# Base URL the backend uses for generating short URLs
# Must match app.short-url.base-domain in application.properties
VITE_SHORT_URL_BASE=http://localhost:8080
