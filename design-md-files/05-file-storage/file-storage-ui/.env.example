# Leave empty when using Vite dev proxy (default). Set for direct API access in production builds.
VITE_API_BASE_URL=

# Comma-separated origins must match app.cors.allowed-origins on the Spring Boot service.
# VITE_API_BASE_URL=https://api.example.com
